# Production server port. Railway injects PORT at runtime; the start script binds
# it via `next start -p ${PORT:-3000}` and defaults to 3000 for local runs.
PORT=3000

# Railway deployment token. NEVER commit a real value — provide it only through
# Railway's secure environment / CI. This file documents the variable name only.
RAILWAY_TOKEN=
